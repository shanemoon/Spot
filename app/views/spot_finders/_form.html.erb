<%= form_for(@spot_finder) do |f| %>
  <% if @spot_finder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spot_finder.errors.count, "error") %> prohibited this spot_finder from being saved:</h2>

      <ul>
      <% @spot_finder.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :longitude %><br />
    <%= f.text_field :longitude %>
  </div>
  <div class="field">
    <%= f.label :latitude %><br />
    <%= f.text_field :latitude %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
